# Harry Potter Explorer 🧙‍♂️

Веб-приложение для изучения магической вселенной Гарри Поттера. Позволяет просматривать информацию о персонажах, факультетах Хогвартса и добавлять любимых героев в избранное.

## Особенности
- **Главная страница:** Атмосферное приветствие.
- **Факультеты:** Информация о четырех факультетах Хогвартса (цвета, символика, качества).
- **Персонажи:** 
    - Проксирование данных через собственный Backend.
    - Поиск по имени.
    - Пагинация.
    - Просмотр подробной информации в модальном окне (палочка, актер, дата рождения и т.д.).
    - Система "Избранного" (сохранение в `localStorage`).
- **Стек технологий:** Python (FastAPI), Jinja2, Vanilla JS, CSS.

## Технический стек и обоснование
Изначально планировалось использовать Node.js (Express) и React. Однако, в рабочей среде был обнаружен только **Python 3.13**, поэтому стек был изменен на:
- **FastAPI:** Современный, быстрый фреймворк для создания API на Python. Идеален для проксирования запросов и обслуживания шаблонов.
- **Jinja2:** Мощный шаблонизатор для Python, позволяющий создавать динамические HTML-страницы.
- **Vanilla JavaScript:** Выбран для фронтенда, чтобы избежать сложности сборки (Webpack/Vite) в условиях ограничений окружения, при этом обеспечивая высокую производительность и интерактивность.
- **CSS3:** Для создания аутентичного "магического" интерфейса с использованием переменных и Grid Layout.
- **OpenAI API:** Для реализации интерактивного чата с персонажами.

## Процесс проектирования и разработки
1.  **Прокси-сервер:** Все запросы к внешнему API (`hp-api.onrender.com`) проходят через Backend на FastAPI. Это скрывает структуру внешнего API и позволяет при необходимости кэшировать данные или изменять их формат.
2.  **UI/UX:** Интерфейс разделен на логические блоки. Для персонажей реализована плиточная сетка (Grid), которая адаптируется под разные размеры экранов.
3.  **Пагинация:** Поскольку API возвращает большой список персонажей, на фронтенде реализована клиентская пагинация для улучшения производительности.
4.  **Чат с ИИ:** Реализован эндпоинт, который принимает имя персонажа и сообщение пользователя. Используется системный промпт, чтобы ИИ отвечал в стиле выбранного персонажа.

## Компромиссы
- **База данных:** Вместо Supabase/Firebase для "Избранного" использован `localStorage`. Это упрощает установку и запуск приложения, не требуя настройки внешних ключей доступа, но сохраняет функционал между сессиями браузера.
- **LLM:** Интегрирован OpenAI GPT-3.5-turbo. Требуется API-ключ.

## Известные проблемы
- Некоторые персонажи в HP-API не имеют изображений (используется плейсхолдер).
- Пагинация реализована на стороне клиента (все персонажи загружаются один раз), что может быть медленным при очень медленном интернете.

## Инструкция по запуску

1. Убедитесь, что у вас установлен Python 3.11+.
2. Установите зависимости:
   ```bash
   pip install fastapi uvicorn httpx jinja2 openai python-dotenv
   ```
3. Настройте API-ключ. Создайте файл `.env` в корневой директории проекта и добавьте туда ваш ключ:
   ```text
   OPENAI_API_KEY=ваш_ключ_здесь
   ```
   Файл `.env` добавлен в `.gitignore` и не попадет в репозиторий при пуше.

4. Запустите сервер:
   ```bash
   python app/main.py
   ```
5. Откройте браузер по адресу: `http://localhost:8000`. 
   При открытии чата приложение проверит наличие ключа и предупредит в окне чата, если он не настроен.
